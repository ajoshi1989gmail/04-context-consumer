# 04-context-consumer

Job that consumes an org Context

**CircleCI entity focus:** Context + Context Env Var API

This repo was auto-generated to exercise a specific slice of the CircleCI API v2 for NHI-governance testing. Link it to CircleCI via the 'Set Up Project' flow (or `POST /organization/{org}/project`) after the GitHub App/OAuth connection is authorized.
